<section class="section text-center blog section-padding bg-gray" data-scroll-index="7">
  <div class="container">
    <div class="row justify-content-center mb-5">
      <div class="col-lg-12">
        <div class="text-center">
          <h2>Latest Posts</h2>
          <p class="text-muted mx-auto section-subtitle mt-3">My articles about technology and leadership.</p>
        </div>
      </div>
    </div>

    <div class="card-deck">
      <% @articles.each do |article| %>
        <div class="card">
          <div class="item">
            <div class="post-img">
              <div class="img">
                <%= link_to article_path(article.slug), "data-turbolinks": "false" do %>
                  <%= image_tag(article.thumbnail.variant(resize_to_fill: [400, 0]), class: 'card-img-top') %>
                <% end %>
              </div>
            </div>
            <div class="card-body">
              <div class="cont">
                <div class="info">
                  <span class="d-none d-lg-block"><em>By</em> Guilherme Pejon</span>
                  <span class="float-left float-lg-right"><%= article.published_at.day %> <%= article.published_at.strftime("%b, %Y") %></span>
                  <%# <a href="#0"><span>06</span> Aug 2018</a> %>
                  <%# <a href="#0"><i class="icon pe-7s-ticket"></i> WordPress</a> %>
                </div>
                <h5><%= link_to article.title, article_path(article.slug), "data-turbolinks": "false" %></h5>
                <%# <a href="#0"><%= article.description %1></a> %>
                <%# <a href="#0" class="more"> %>
                  <%#   <span>Read More</span> %>
                  <%# </a> %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <div class="row mt-3">
      <ul class="col list-unstyled list-inline mb-4 text-uppercase work_menu">
        <li class="list-inline-item">
          <%= link_to "MORE POSTS", articles_path, 'data-turbolinks': 'false' %>
        </li>
      </ul>
    </div>
  </div>
</section>
